[pytest]
pythonpath = src .
testpaths = tests
addopts = --ignore-glob=*.local.py --strict-markers --junitxml=artifacts/junit.xml --html=artifacts/report.html
markers =
    app: braucht die laufende Zielanwendung auf einem angemeldeten Desktop
